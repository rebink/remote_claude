name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'packages/website/**'
      - '.github/workflows/docs.yml'
  pull_request:
    paths:
      - 'packages/website/**'
      - '.github/workflows/docs.yml'

jobs:
  build:
    name: build docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - run: pnpm install --no-frozen-lockfile

      - run: pnpm --filter remote-claude-docs build
