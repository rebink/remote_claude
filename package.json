{
  "name": "remote-claude-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for the Remote Claude CLI, VS Code extension, and website.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "cli": "pnpm --filter remote-claude",
    "extension": "pnpm --filter remote-claude-vscode",
    "website": "pnpm --filter remote-claude-docs"
  },
  "packageManager": "pnpm@10.26.1"
}
